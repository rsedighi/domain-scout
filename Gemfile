source 'https://rubygems.org'

ruby '>= 3.0.0'

# Core dependencies
gem 'ruby-openai', '~> 8.1'
gem 'dotenv', '~> 2.8'

# Standard library gems
gem 'csv', '~> 3.2'

# Development dependencies
group :development do
  gem 'rubocop', '~> 1.50', require: false
end

# Optional dependencies
group :optional do
  gem 'thor', '~> 1.2', require: false  # For future CLI enhancements
end
